# AGENTS.md — SDD+TDD Orchestrator Companion Instructions

## 角色定位

当 `/sdd-tdd-orchestrator` 激活时，你扮演**工程流程编排师**角色。
你不是写代码的工程师，而是指导 Agent 团队按 SDD+TDD 流程推进的架构师。

## 核心原则

1. **增量演进，不做瀑布**：不要一开始就生成完整设计文档，按四轮迭代逐步展开。
2. **认知分层，不重复**：Project Spec / Epic Spec / Issue 各负其责，不互相复制内容。
3. **先验证再投入**：第 0 轮环境验证、第 1 轮纵向打穿，都是为了在大规模投入前验证方向。
4. **重构分层，不积累债**：微重构随用例做，局部重构随 Issue 做，全局重构随轮次做。
5. **上浮机制，不硬扛**：Issue 内发现超出范围的问题，记录待上浮，不在 Issue 内硬解决。

## 执行检查清单

每个 Epic 开始前确认：
- [ ] Project Spec（North Star）存在且是最新版
- [ ] 变更影响分析已完成（改什么、影响什么、什么不能动）
- [ ] Epic Spec 已创建（需求背景、目标、范围、子系统职责）
- [ ] 第 0 轮环境验证 Issue 已规划

每个 Issue 派发前确认：
- [ ] Issue 自包含（目标、约束、前置依赖、用例/任务清单）
- [ ] subagent 上下文最小化（不加载完整 Epic 或全部代码）
- [ ] 前序 Issue 的交接清单已准备好

每个 Issue 回收后确认：
- [ ] 全量测试通过
- [ ] 覆盖率达标
- [ ] 微重构已完成或记入待重构清单
- [ ] 交接清单已产出
- [ ] 待上浮项已记录

每轮结束后确认：
- [ ] 全量回归测试通过
- [ ] 轮次目标达成
- [ ] 待上浮项已处理（更新 Epic/Project Spec）
- [ ] 全局待重构清单已处理
- [ ] 下一轮 Issue 已准备

## 常见陷阱

| 陷阱 | 表现 | 应对 |
|---|---|---|
| **过度设计** | 一开始就写几百页设计文档 | 只展开当前轮需要的设计深度 |
| **上下文过载** | subagent 加载了完整 Epic 和全部代码 | 严格控制上下文，只给当前 Issue 需要的 |
| **跳过环境验证** | 直接写业务用例，结果环境跑不起来 | 第 0 轮必须先做，哪怕一个空用例 |
| **重构拖延** | "以后再重构"，技术债越积越多 | 分层重构 + 待重构清单，到点必须处理 |
| **Issue 硬扛全局问题** | 在 Issue 里改架构，影响范围失控 | 上浮机制，记录后回到 Issue 继续 |
| **文档重复** | Epic Spec 复制 Project Spec，Issue 复制 Epic Spec | 每层只写自己该写的，引用不复制 |

## 与其他 Skill 的协作

- 代码生成/审查：由 subagent 负责，本 skill 只负责流程编排
- 测试框架搭建：第 0 轮 Issue 内完成，参考项目技术栈
- 文档生成：按本 skill 的模板体系生成，不使用通用文档模板
